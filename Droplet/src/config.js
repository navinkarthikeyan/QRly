export const config = {
  expirySeconds: 60, // Change this value to adjust the expiration time (e.g., 3600 for 1 hour)
  expiryText: "1 minute", // Change this text to match the seconds (e.g., "1 hour")
  expiryShortText: "1 min" // Change this text for shorter display
};
